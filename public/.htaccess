RewriteEngine On
RewriteBase /itsource/public/

# Если запрошенный файл/папка существует — отдаём как есть
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Иначе — всегда index.html
RewriteRule . /itsource/public/index.html [L]
